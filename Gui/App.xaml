<Application x:Class="Gui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Gui"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ControlTemplate x:Key="MiejsceTemplate" TargetType="Button">
            <Grid>
                <Rectangle x:Name="SeatRectangle" Fill="{TemplateBinding Background}" Stroke="Black" StrokeThickness="1"/>
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="SeatRectangle" Property="Fill" Value="DarkGray"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="SeatRectangle" Property="Fill" Value="Black"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Application.Resources>
    
</Application>
